# Task 0 PPO

报告里写的是 PPO clip loss 实现和 `validate_ppo_implementation()` 校验通过。

| 内容 | 文件 |
|---|---|
| PPO loss 代码 | `../../scripts/0-ppo.py` |
| loss / grad 校验 | `../../scripts/0-ppo.py` 里的 `validate_ppo_implementation()` |

如果要复查，可以直接运行：

```bash
python3 -X utf8 lab3/scripts/0-ppo.py
```

目前没有另外保存运行截图或日志，因为这个任务主要就是代码校验。
