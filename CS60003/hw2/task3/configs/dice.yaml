experiment:
  name: task3_unet_dice
  seed: 42
  output_root: hw2/task3/outputs

train:
  loss: dice
  epochs: 80
  lr: 0.0002
  weight_decay: 0.0001
  early_stopping_patience: 16

logging:
  swanlab:
    enabled: true
    project: cs60003-hw2-task3
    group: loss-comparison
    tags: [hw2, task3, stanford-background, unet, dice]
    description: U-Net from scratch with hand-written Dice Loss.
